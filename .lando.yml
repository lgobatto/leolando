
# Sage configuration start
services:
  appserver:
    run:
      - "bash scripts/sage-setup.sh"

tooling:
  sage-build:
    description: "Build the Sage theme"
    dir: /app/wp-content/themes/gui_design
    cmd:
      - appserver: composer install
      - node: yarn install
      - node: yarn build
# Sage configuration end
